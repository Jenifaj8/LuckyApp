@{
    ViewData["Title"] = "Keno";
}

<div class="py-5" style="background-color: #f4efff; min-height: 100vh;">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-8">
                <div class="card shadow-sm border-0 rounded-4">
                    <div class="card-body p-5">
                        <h1 class="mb-3">Keno</h1>
                        <p class="text-muted mb-4">
                            Enter numbers and generate a new pattern-based combination.
                        </p>

                        <form method="post">
                            <div class="mb-3">
                                <label class="form-label fw-semibold">Enter numbers (comma separated)</label>
                                <input type="text" name="numbers" class="form-control form-control-lg rounded-3"
                                       placeholder="e.g. 2,8,14,21,33,40,55,67" />
                            </div>

                            <button type="submit" class="btn btn-primary rounded-3 px-4">Generate</button>
                        </form>

                        @if (ViewBag.Result != null)
                        {
                            <div class="alert alert-light border mt-4 rounded-3">
                                <h4 class="mb-2">Generated Numbers</h4>
                                <p class="mb-0 fs-5 fw-semibold">@ViewBag.Result</p>
                            </div>
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>